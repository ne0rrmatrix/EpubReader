<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup
    x:Class="EpubReader.Views.SettingsPage"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    x:Name="CurrentPage"
    x:DataType="SettingsPageViewModel"
    x:TypeArguments="system:Boolean"
    BackgroundColor="{AppThemeBinding Dark={StaticResource AppBackgroundDarkColor},
                                      Light=White}"
    CanBeDismissedByTappingOutsideOfPopup="True"
    HeightRequest="{OnPlatform WinUI=550,
                               Default=450}"
    Loaded="CurrentPage_Loaded"
    Unloaded="CurrentPage_Unloaded"
     WidthRequest="330">

    <ScrollView x:Name="ScrollView" VerticalScrollBarVisibility="Never" HorizontalScrollBarVisibility="Never" >
        <Border Style="{StaticResource BorderGalleryCard}" Margin="5" Padding="5">
            <VerticalStackLayout
                x:Name="grid"
                Padding="0"
                Spacing="20">

        <!--  Reader Appearance Section  -->
        <StackLayout Grid.Row="0" Spacing="6" HorizontalOptions="Center">
            <Picker
                x:Name="ThemePicker"
                Title="Theme"
                ItemDisplayBinding="{Binding Name, x:DataType=ColorScheme}"
                ItemsSource="{Binding ColorSchemes}"
                SelectedIndexChanged="ThemePicker_SelectedIndexChanged"
                WidthRequest="250" />

            <CollectionView
                x:Name="ThemePreview"
                ItemsSource="{Binding ColorSchemes}"
                SelectionMode="Single"
                SelectionChanged="ThemePreview_SelectionChanged"
                HeightRequest="96"
                ItemsLayout="HorizontalList">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="ColorScheme">
                        <Border Padding="6" Margin="6" StrokeShape="RoundRectangle 6" BackgroundColor="{Binding BackgroundColor}" Stroke="Transparent">
                            <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
                                <Label Text="Aa" FontSize="20" TextColor="{Binding TextColor}" HorizontalOptions="Center" />
                                <Label Text="{Binding Name}" FontSize="11" TextColor="{StaticResource Gray600}" HorizontalOptions="Center" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>

        <StackLayout Grid.Row="1" Spacing="6" HorizontalOptions="Center">
            <Picker
                x:Name="FontPicker"
                Title="Select Font"
                ItemDisplayBinding="{Binding FontFamily, x:DataType=EpubFonts}"
                ItemsSource="{Binding Fonts}"
                SelectedIndexChanged="FontPicker_SelectedIndexChanged"
                WidthRequest="250" />

            <Label
                x:Name="FontPreview"
                Text="The quick brown fox jumps over the lazy dog"
                HorizontalOptions="Center"
                LineBreakMode="NoWrap"
                FontSize="16" />
        </StackLayout>

        <Label
            Grid.Row="2"
            FontAttributes="Bold"
            HorizontalOptions="Center"
            Text="Select Font Size" />

        <Slider
            x:Name="FontSizeSlider"
            Grid.Row="3"
            Maximum="36"
            Minimum="8"
            ValueChanged="OnFontSizeSliderChanged"
            WidthRequest="250" />

        <Label
            x:Name="FontSizeLabel"
            Grid.Row="4"
            x:DataType="{x:Type Slider}"
            HorizontalOptions="Center"
            Text="{Binding Source={x:Reference FontSizeSlider}, Path=Value, StringFormat='Font Size: {0:F0}'}" />

        <StackLayout Grid.Row="5" Spacing="6" HorizontalOptions="Center">
            <Label
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Text="Line Spacing" />

            <Picker
                x:Name="LineSpacingPicker"
                Title="Line Spacing"
                SelectedIndexChanged="LineSpacingPicker_SelectedIndexChanged"
                WidthRequest="250" />
        </StackLayout>

        <StackLayout Grid.Row="6" Spacing="6" HorizontalOptions="Center">
            <Label
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Text="Paragraph Spacing" />

            <Picker
                x:Name="ParagraphSpacingPicker"
                Title="Paragraph Spacing"
                SelectedIndexChanged="ParagraphSpacingPicker_SelectedIndexChanged"
                WidthRequest="250" />
        </StackLayout>

        <StackLayout Grid.Row="7" Spacing="6" HorizontalOptions="Center">
            <Label
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Text="Text Alignment" />

            <Picker
                x:Name="TextAlignmentPicker"
                Title="Text Alignment"
                SelectedIndexChanged="TextAlignmentPicker_SelectedIndexChanged"
                WidthRequest="250" />
        </StackLayout>

        <StackLayout Grid.Row="8" Spacing="6" HorizontalOptions="Center">
            <Label
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Text="Hyphenation" />

            <Picker
                x:Name="HyphenationPicker"
                Title="Hyphenation"
                SelectedIndexChanged="HyphenationPicker_SelectedIndexChanged"
                WidthRequest="250" />
        </StackLayout>

        <StackLayout Grid.Row="9" Spacing="6" HorizontalOptions="Center">
            <Label
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Text="Letter Spacing" />

            <Picker
                x:Name="LetterSpacingPicker"
                Title="Letter Spacing"
                SelectedIndexChanged="LetterSpacingPicker_SelectedIndexChanged"
                WidthRequest="250" />
        </StackLayout>

        <StackLayout Grid.Row="10" Spacing="6" HorizontalOptions="Center">
            <Label
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Text="Word Spacing" />

            <Picker
                x:Name="WordSpacingPicker"
                Title="Word Spacing"
                SelectedIndexChanged="WordSpacingPicker_SelectedIndexChanged"
                WidthRequest="250" />
        </StackLayout>

        <HorizontalStackLayout
            x:Name="stackLayout"
            Grid.Row="11"
            HorizontalOptions="Center"
            IsVisible="{OnPlatform WinUI=true,
                                   Default=false}"
            Spacing="100"
            WidthRequest="250">
            <Label Text="Multiple Columns" VerticalOptions="Center" />
            <Switch
                x:Name="switchControl"
                Toggled="switchControl_Toggled"
                VerticalOptions="Center" />
        </HorizontalStackLayout>

        <!--  Authentication Section  -->
        <Label
            Grid.Row="12"
            FontAttributes="Bold"
            HorizontalOptions="Center"
            Text="Account &amp; Sync" />

        <VerticalStackLayout
            Grid.Row="13"
            Spacing="10">
            
            <!--  Auth Status Display  -->
            <Label
                FontSize="13"
                HorizontalOptions="Center"
                HorizontalTextAlignment="Center"
                IsVisible="{Binding IsAuthenticated}"
                LineBreakMode="WordWrap"
                Text="{Binding AuthStatusText}"
                TextColor="{AppThemeBinding Dark={StaticResource Gray300},
                                            Light={StaticResource Gray600}}" />

            <Label
                FontSize="13"
                HorizontalOptions="Center"
                HorizontalTextAlignment="Center"
                IsVisible="{Binding IsLocalOnly}"
                Text="Local mode - No cloud sync"
                TextColor="{AppThemeBinding Dark={StaticResource Gray400},
                                            Light={StaticResource Gray600}}" />

            <!--  Sign In Button (visible when not authenticated)  -->
            <Button
                Command="{Binding SignInCommand}"
                HeightRequest="40"
                HorizontalOptions="Center"
                IsVisible="{Binding IsNotAuthenticated}"
                Text="Sign In"
                BackgroundColor="{StaticResource NormalButtonBackgroundColor}"
                TextColor="{StaticResource NormalButtonTextColor}" />

            <!--  Sign Out Button (visible when authenticated)  -->
            <Button
                Command="{Binding SignOutCommand}"
                HeightRequest="40"
                HorizontalOptions="Center"
                IsVisible="{Binding IsAuthenticated}"
                Text="Sign Out"
                WidthRequest="200"
                BackgroundColor="{StaticResource NormalButtonBackgroundColor}"
                TextColor="{StaticResource NormalButtonTextColor}" />

            <!--  Delete Cloud Data (visible when authenticated)  -->
            <Button
                HeightRequest="40"
                HorizontalOptions="Center"
                Text="Export Data"
                Clicked="OnExportDataClicked"
                WidthRequest="200"
                BackgroundColor="{StaticResource NormalButtonBackgroundColor}"
                TextColor="{StaticResource NormalButtonTextColor}" />

            <Button
                Grid.Row="9"
                HeightRequest="40"
                HorizontalOptions="Center"
                Text="Privacy Policy"
                Clicked="OnPrivacyClicked"
                WidthRequest="200"
                BackgroundColor="{StaticResource NormalButtonBackgroundColor}"
                TextColor="{StaticResource NormalButtonTextColor}" />

            <Button
                Grid.Row="5"
                Clicked="RemoveAllSettings"
                HeightRequest="45"
                Text="Restore Default Settings"
                WidthRequest="200"
                BackgroundColor="{StaticResource NormalButtonBackgroundColor}"
                TextColor="{StaticResource NormalButtonTextColor}" />

            <Button
                Command="{Binding DeleteCloudDataCommand}"
                HeightRequest="40"
                HorizontalOptions="Center"
                IsVisible="{Binding IsAuthenticated}"
                Text="Delete Cloud Data"
                WidthRequest="200"
                BackgroundColor="{StaticResource Reset}"
                TextColor="{StaticResource White}"
                BorderColor="{StaticResource Reset-Dark}" />

            <Button
                HeightRequest="40"
                HorizontalOptions="Center"
                Text="Delete Local Data"
                Clicked="OnDeleteLocalDataClicked"
                WidthRequest="200"
                BackgroundColor="{StaticResource Reset}"
                TextColor="{StaticResource White}"
                BorderColor="{StaticResource Reset-Dark}" />

        </VerticalStackLayout>

            </VerticalStackLayout>
        </Border>
    </ScrollView>

</toolkit:Popup>
