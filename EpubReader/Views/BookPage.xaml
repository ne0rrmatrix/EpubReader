<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="EpubReader.Views.BookPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    xmlns:local="clr-namespace:EpubReader.Views"
    xmlns:model="clr-namespace:EpubReader.Models"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewModel="clr-namespace:EpubReader.ViewModels"
    x:Name="CurrentPage"
    x:DataType="viewModel:BookViewModel"
    Loaded="CurrentPage_Loaded"
    SafeAreaEdges="None"
    Shell.NavBarIsVisible="False"
    Shell.BackgroundColor="{AppThemeBinding Dark={StaticResource AppBackgroundDarkColor},
                                            Light={OnPlatform WinUI={StaticResource White},
                                                              Default={StaticResource Blue200Accent}}}"
    Shell.TabBarIsVisible="False">
    <ContentPage.ToolbarItems>
        <ToolbarItem
            BindingContext="{Binding Path=BindingContext, Source={x:Reference CurrentPage}, x:DataType=ContentPage}"
            Command="{Binding ShowPopupCommand}"
            IconImageSource="{AppThemeBinding Light={OnPlatform WinUI=settings_darkmode.png,
                                                                Default=settings_lightmode.png},
                                              Dark=settings_lightmode.png}"
            Order="Primary" />
        <ToolbarItem
            Clicked="ToggleChapterDrawer"
            IconImageSource="{AppThemeBinding Light=book_open_light_mode.png,
                                              Dark=book_open_dark_mode.png}"
            Order="Primary"/>
    </ContentPage.ToolbarItems>

    <Grid SafeAreaEdges="None">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Reader controls remain behind the scaled page so the top options are revealed when controls open.  -->
        <ScrollView
            x:Name="menu"
            Grid.Row="0"
            HorizontalScrollBarVisibility="Never"
            IsVisible="False"
            SafeAreaEdges="None"
            VerticalScrollBarVisibility="Never"
            ZIndex="0">
            <Grid
                BackgroundColor="{AppThemeBinding Dark={StaticResource AppBackgroundDarkColor},
                                                  Light={StaticResource White}}"
                SafeAreaEdges="None">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
               
                <Label
                    x:Name="BookTitle"
                    Grid.Row="0"
                    Margin="0,10,0,0"
                    FontAttributes="Bold"
                    FontSize="{OnPlatform Android=20,
                                          iOS=20,
                                          WinUI=24,
                                          MacCatalyst=24}"
                    HorizontalOptions="Center"
                    Text="{Binding BookTitle}"
                    TextColor="{AppThemeBinding Dark={StaticResource White},
                                                Light={StaticResource Black}}" />
            </Grid>
        </ScrollView>

        <!--  Virtualized chapter drawer. The CollectionView only creates visible rows.  -->
        <Grid
            x:Name="chapterDrawer"
            Grid.Row="0"
            BackgroundColor="#66000000"
            IsVisible="False"
            SafeAreaEdges="None"
            ZIndex="10"
            VerticalOptions="Fill">
            <BoxView BackgroundColor="Transparent">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer Tapped="CloseMenuAsync" />
                </BoxView.GestureRecognizers>
            </BoxView>
            <Border
                Margin="0"
                Padding="16,18"
                BackgroundColor="{AppThemeBinding Dark={StaticResource AppBackgroundDarkColor},
                                                  Light={StaticResource White}}"
                HorizontalOptions="End"
                MaximumWidthRequest="360"
                StrokeThickness="0"
                ZIndex="1"
                VerticalOptions="Fill">
                <Grid RowDefinitions="Auto,Auto,*" RowSpacing="12">
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout Spacing="2">
                            <Label
                                FontAttributes="Bold"
                                FontSize="{OnPlatform Android=20,
                                                      iOS=20,
                                                      WinUI=24,
                                                      MacCatalyst=24}"
                                Text="{Binding BookTitle}"
                                TextColor="{AppThemeBinding Dark={StaticResource White},
                                                            Light={StaticResource Black}}"
                                LineBreakMode="TailTruncation" />
                            <Label
                                x:Name="currentChapterLabel"
                                FontSize="13"
                                TextColor="{AppThemeBinding Dark={StaticResource Gray300},
                                                            Light={StaticResource Gray600}}"
                                LineBreakMode="TailTruncation" />
                        </VerticalStackLayout>
                    </Grid>
                    <Label
                        x:Name="chapterCountLabel"
                        Grid.Row="1"
                        FontSize="13"
                        TextColor="{AppThemeBinding Dark={StaticResource Gray300},
                                                    Light={StaticResource Gray600}}" />
                    <CollectionView
                        x:Name="chapterList"
                        Grid.Row="2"
                        SelectionChanged="ChapterList_SelectionChanged"
                        SelectionMode="Single">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="{x:Type local:ChapterNavigationItem}">
                                <Border
                                    Margin="0,2"
                                    Padding="12,10"
                                    StrokeShape="RoundRectangle 8"
                                    StrokeThickness="0">
                                    <Label
                                        FontSize="16"
                                        Text="{Binding Title}"
                                        TextColor="{AppThemeBinding Dark={StaticResource White},
                                                                    Light={StaticResource Black}}" />
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Grid>
            </Border>
        </Grid>

        <!--  Main content grid holding WebView and page info  -->
        <Grid
            x:Name="grid"
            Grid.Row="0"
            BackgroundColor="{AppThemeBinding Dark={StaticResource AppBackgroundDarkColor},
                                              Light={StaticResource White}}"
            SafeAreaEdges="None"
            ZIndex="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="40" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <WebView
                x:Name="webView"
                Grid.Row="0"
                Navigated="webView_Navigated"
                Scale="1"
                Source="{Binding Source}" />

            <Label
                x:Name="pageLabel"
                Grid.Row="2"
                FontSize="{OnPlatform Android=16,
                                      iOS=16,
                                      WinUI=20,
                                      MacCatalyst=20}"
                HeightRequest="30"
                HorizontalOptions="Center"/>
           
            <!--  Native loading overlay shown while chapter renders and native settings apply  -->
            <Grid
                x:Name="NativeLoadingOverlay"
                Grid.RowSpan="3"
                BackgroundColor="{AppThemeBinding Dark={StaticResource OverlayDarkColor},
                                                  Light={StaticResource OverlayLightColor}}"
                HorizontalOptions="Fill"
                IsVisible="False"
                SafeAreaEdges="None"
                VerticalOptions="Fill">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.6*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Cover area: occupies ~60% of overlay height and centers the image  -->
                <Grid
                    Grid.Row="0"
                    HorizontalOptions="Center"
                    SafeAreaEdges="None"
                    VerticalOptions="Center">
                    <Image
                        Aspect="AspectFit"
                        HorizontalOptions="Center"
                        Source="{Binding CoverImage}"
                        VerticalOptions="Center" />
                </Grid>

                <!--  Activity indicator centered below the cover  -->
                <HorizontalStackLayout
                    Grid.Row="1"
                    Padding="0,10,0,0"
                    HorizontalOptions="Center"
                    SafeAreaEdges="None"
                    Spacing="8"
                    VerticalOptions="Center">
                    <ActivityIndicator
                        HeightRequest="40"
                        IsRunning="True"
                        WidthRequest="40"
                        Color="{AppThemeBinding Dark={StaticResource White},
                                                Light={StaticResource Black}}" />
                </HorizontalStackLayout>

                <!--  Loading text  -->
                <Label
                    Grid.Row="2"
                    Margin="0,6,0,0"
                    FontAttributes="Bold"
                    HorizontalOptions="Center"
                    Text="Loading chapter..."
                    TextColor="{AppThemeBinding Dark={StaticResource White},
                                                Light={StaticResource Black}}"
                    VerticalOptions="Start" />
            </Grid>
        </Grid>

        <!--
            Slider container moved OUTSIDE the WebView grid and placed in its own Auto row
            This ensures the slider is a separate visual element below the page (not on top of it).
        -->
        <VerticalStackLayout
            x:Name="sliderContainer"
            Grid.Row="1"
            Padding="12,8"
            BackgroundColor="Transparent"
            HorizontalOptions="Fill"
            IsVisible="False"
            SafeAreaEdges="None"
            Spacing="8"
            VerticalOptions="Center">

            <Slider
                x:Name="pageSlider"
                DragCompleted="PageSlider_DragCompleted"
                DragStarted="PageSlider_DragStarted"
                HeightRequest="36"
                Maximum="1"
                MaximumTrackColor="{AppThemeBinding Dark={StaticResource SliderMaximumTrackDarkColor},
                                                    Light={StaticResource SliderMaximumTrackLightColor}}"
                Minimum="0"
                MinimumTrackColor="{AppThemeBinding Dark={StaticResource Blue200Accent},
                                                    Light={StaticResource Blue600}}"
                ThumbColor="{AppThemeBinding Dark={StaticResource White},
                                             Light={StaticResource Blue600}}"
                ValueChanged="PageSlider_ValueChanged"
                Value="0" />
            <Label
                x:Name="sliderPageLabel"
                Grid.Row="2"
                FontSize="{OnPlatform Android=16,
                                      iOS=16,
                                      WinUI=20,
                                      MacCatalyst=20}"
                HeightRequest="30"
                HorizontalOptions="Center"
                TextColor="{AppThemeBinding Dark={StaticResource White},
                                            Light={StaticResource Black}}" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>